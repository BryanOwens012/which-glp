[phases.setup]
nixPkgs = ["python313", "libpq"]

[phases.install]
cmds = [
    "cd ../.. && pip install -r requirements.txt"
]

[phases.build]
cmds = ["echo 'No build step required'"]

[start]
cmd = "python3 api.py"
